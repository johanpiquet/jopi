# Jopi

`jopi` is a CLI tool for Jopi.

It allows installing new projects from templates.  
Type: `npx jopi init` and select the template you want.

Commande "jopi init": env var "JOPI_INIT_USE_DEV_DIR" allows downloading from a dir.